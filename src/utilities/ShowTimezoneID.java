package utilities;

/** ShowTimezoneID lambda Interface. The purpose of this lambda is to set the timezone for display on the main menu.
 * Lambda interface allows for the general use of displaying a timezone wherever the user wants. */
public interface ShowTimezoneID {
    void displayTimeZoneID();
}
